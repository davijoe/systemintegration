# gRPC

This is an example of a client and server that can talk to eachother using gRPC

Open them with these commands in their own respective terminal:

```bash
uv run python server.py
uv run python client.py
```

Now write any message in terrminal runnning the client.
